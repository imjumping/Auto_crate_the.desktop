# Auto_crate_the.desktop

> create是正确的，crate错了 不改了

一个用于创建 `.desktop` 启动器文件的 Qt 图形工具。

## 编译

```bash
mkdir build && cd build
cmake ..
make
./DesktopLauncher
```

>依赖

>>Qt6 (Core, Gui, Widgets)

>>CMake >= 3.16

>>C++17 支持
